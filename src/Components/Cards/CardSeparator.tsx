import React from 'react';
import {View, StyleSheet} from 'react-native';

export const CardSeparator = () => <View style={styles.container} />;

const styles = StyleSheet.create({
  container: {
    height: 10,
  },
});
